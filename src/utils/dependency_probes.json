{
  "maven": {
    "probe": "mvn -v",
    "version_regex": "Apache Maven (?P<version>[0-9.]+)"
  },
  "golang": {
    "probe": "go version",
    "version_regex": "go version go(?P<version>[0-9.]+)"
  }
}
